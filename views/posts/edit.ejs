<%- include('../partials/header', { title: 'Редактировать пост' }) %>

<div class="post-form-page">

    <div class="post-form-box">

        <h1 class="post-form-title">
            EDIT POST
        </h1>

        <p class="post-form-subtitle">
            Редактирование поста
        </p>

        <form
                action="/posts/<%= post._id %>/edit"
                method="POST"
                class="post-form"
        >

            <div class="form-group">

                <label>
                    Заголовок
                </label>

                <input
                        type="text"
                        name="title"
                        value="<%= post.title %>"
                        required
                >

            </div>

            <div class="form-group">

                <label>
                    Категория
                </label>

                <select name="category">

                    <% categories.forEach(category => { %>

                        <option value="<%= category %>">
                            <%= category %>
                        </option>

                    <% }) %>

                </select>

            </div>

            <div class="form-group">

                <label>
                    Статус
                </label>

                <select name="status">

                    <% statuses.forEach(status => { %>

                        <option value="<%= status %>">
                            <%= status %>
                        </option>

                    <% }) %>

                </select>

            </div>



            <div class="form-group">

                <label>
                    Описание
                </label>

                <textarea
                        name="description"
                        required
                ><%= post.content %></textarea>

            </div>

            <div class="edit-actions">

                <button
                        type="submit"
                        class="save-btn"
                >
                    Сохранить
                </button>

                <a
                        href="/posts/<%= post._id %>"
                        class="cancel-btn"
                >
                    Отмена
                </a>

            </div>

        </form>

    </div>

</div>

<%- include('../partials/footer') %>